# Comic Web 功能需求列表

## 已完成功能 (✓)

### 核心功能
- [x] 漫画目录扫描与展示
  - 自动扫描 `comics/` 目录下的子目录作为漫画列表
  - 在主页展示漫画封面和名称
- [x] 漫画阅读功能
  - 点击漫画进入阅读页面
  - 按顺序展示漫画图片
- [x] 安全防护
  - 校验文件路径，防止目录遍历攻击
- [x] 多格式图片支持
  - 支持 JPG/PNG/GIF/BMP/WEBP 等常见图片格式
- [x] 响应式设计
  - 适配不同设备屏幕尺寸
- [x] PyInstaller 打包功能
  - 将应用打包为独立的可执行文件
- [x] 日志记录功能
  - 记录应用运行状态和错误信息
  - 支持文件和控制台输出
  - 支持日志轮转
- [x] 目录结构重构
  - 模块化项目结构
  - 清晰的代码组织
- [x] SQLite 数据库支持
  - 存储漫画元数据（标题、作者、标签、描述）
  - 存储用户账户信息
  - 存储书签信息
- [x] 用户认证系统
  - 登录/登出功能
  - 默认管理员账户（用户名: admin, 密码: admin）
  - 注册功能（可配置启用/禁用）

### API 接口
- [x] `/` - 主页，返回漫画列表
- [x] `/login` - 登录页面
- [x] `/logout` - 登出接口
- [x] `/register` - 用户注册接口
- [x] `/comic/<name>` - 漫画阅读页面
- [x] `/api/comics` - 获取所有漫画信息（JSON格式）
- [x] `/api/comic/<name>/images` - 获取漫画图片列表（分页）
- [x] `/comics/<name>/<filename>` - 提供具体图片文件
- [x] `/comic/<name>/cover` - 提供漫画封面图片

### 管理功能
- [x] 漫画元数据管理（标题、作者、标签、描述）
- [x] 书签功能
- [x] 用户账户系统

## 打包和部署

### 如何打包应用

1. 确保已安装 PyInstaller:
   ```
   pip install pyinstaller
   ```

2. 在项目根目录运行构建脚本:
   ```
   python build_exe.py
   ```

3. 构建完成后，可执行文件将位于 `dist/ComicWeb.exe`

### 运行打包后的应用

1. 确保 `comics` 目录与 `ComicWeb.exe` 文件在同一目录下
2. 双击运行 `ComicWeb.exe`
3. 在浏览器中访问 `http://localhost:5000`
4. 使用默认账户登录（用户名: admin, 密码: admin）

### 配置文件支持

应用支持通过 `comic_web.conf` 配置文件进行配置：

```ini
[app]
enable_registration = false

[logging]
log_level = ERROR
log_file_max_bytes = 10485760
log_file_backup_count = 5
log_enable_console = true
log_enable_file = true
```

配置文件应放置在与可执行文件相同的目录中。

### 常见问题及解决方案

#### 模板文件找不到错误
如果在运行打包后的应用时遇到 `jinja2.exceptions.TemplateNotFound: index.html` 错误，这通常是因为 PyInstaller 没有正确打包模板文件。构建脚本已修复此问题，确保使用最新版本的构建脚本重新打包。

#### 登录问题
首次运行应用时，请使用默认管理员账户登录：
- 用户名: admin
- 密码: admin

登录后可以创建新用户或修改密码。

## 待开发功能 (☐)

### 用户体验改进
- [ ] 添加页面导航功能（跳转到指定页面）
- [ ] 添加键盘快捷键支持（左右箭头翻页）
- [ ] 添加全屏阅读模式
- [ ] 添加图片缩放功能
- [ ] 添加阅读进度指示器
- [ ] 添加暗色/亮色主题切换

### 性能优化
- [x] 大图片加载性能优化
  - 图片懒加载（原生loading="lazy"属性）
  - 图片尺寸预获取和缓存
  - CSS优化避免重排

### 部署和配置
- [x] 添加配置文件支持
- [ ] 添加自定义端口和主机设置
- [ ] 添加Docker支持
- [ ] 添加HTTPS支持

## 未来扩展功能 (○)

- [ ] 多用户支持和权限管理
- [ ] 云端同步功能
- [ ] 漫画下载功能
- [ ] 支持在线漫画源